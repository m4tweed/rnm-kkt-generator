# 🚀 Инструкция по публикации на GitHub Pages

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите кнопку "New repository" (зеленая кнопка)
3. Введите название репозитория: `rnm-generator`
4. Добавьте описание: "РНМ ККТ генератор и расшифровщик"
5. Выберите "Public" (для GitHub Pages)
6. НЕ добавляйте README, .gitignore или лицензию (они уже есть)
7. Нажмите "Create repository"

## Шаг 2: Загрузка кода

Выполните команды в терминале:

```bash
cd /Users/a.matveev/rnm_generator
git remote add origin https://github.com/YOUR_USERNAME/rnm-generator.git
git branch -M main
git push -u origin main
```

Замените `YOUR_USERNAME` на ваш GitHub username.

## Шаг 3: Настройка GitHub Pages

1. Перейдите в настройки репозитория (вкладка "Settings")
2. В левом меню найдите "Pages"
3. В разделе "Source" выберите "Deploy from a branch"
4. Выберите ветку "main" и папку "/ (root)"
5. Нажмите "Save"

## Шаг 4: Готово! 🎉

Через несколько минут ваш сайт будет доступен по адресу:
`https://YOUR_USERNAME.github.io/rnm-generator/`

## 📁 Структура проекта

```
rnm_generator/
├── index.html          # Основной файл сервиса
├── README.md           # Описание проекта
└── .playwright-mcp/    # Скриншоты (можно удалить)
```

## 🔧 Готовые команды

Если у вас есть GitHub CLI (`gh`), можете использовать:

```bash
cd /Users/a.matveev/rnm_generator
gh repo create rnm-generator --public --source=. --remote=origin --push
gh api repos/:owner/rnm-generator/pages -X POST -f source[branch]=main -f source[path]=/
```

## ⚠️ Важные моменты

- Убедитесь, что репозиторий публичный (для бесплатного GitHub Pages)
- Файл `index.html` должен быть в корне репозитория
- GitHub Pages может занять до 10 минут для первого развертывания
- После изменений в коде просто сделайте `git push` - сайт обновится автоматически
